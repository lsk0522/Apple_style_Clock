<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The clock surface is always black, never the system appearance, so this
         does not inherit the DayNight theme the rest of the app uses. -->
    <style name="Theme.Nightstand.Standby" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>
